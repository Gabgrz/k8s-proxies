apiVersion: v1
kind: Service
metadata:
  name: envoy-proxy
  annotations:
    networking.gke.io/load-balancer-type: "Internal"
spec:
  selector:
    app: envoy-proxy
  type: LoadBalancer
  ports:
  - name: envoy-proxy
    protocol: TCP
    port: 6443
    targetPort: 6443
