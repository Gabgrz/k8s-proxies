apiVersion: v1
kind: Service
metadata:
  name: haproxy-service
  annotations:
    networking.gke.io/load-balancer-type: "Internal"
spec:
  selector:
    app: haproxy-server
  type: LoadBalancer
  ports:
  - name: haproxy-service
    protocol: TCP
    port: 6443
    targetPort: 6443
